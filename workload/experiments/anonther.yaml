apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: another
  name: another
spec:
  replicas: 3
  selector:
    matchLabels:
      app: default
  template:
    metadata:
      labels:
        app: default
    spec:
      containers:
      - image: nginx:latest
        name: nginx
